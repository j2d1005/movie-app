import React from "react";
import styled from "styled-components";

const Text = styled.p`
  
`;

const NoMovies = ({ id, title, desc, poster }) => {
    return (
        <Text>
            
        </Text>
    )
    
}


export default NoMovies;